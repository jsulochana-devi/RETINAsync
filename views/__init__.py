# views/__init__.py — RETINAsync UI page modules
# Note: This directory is intentionally named 'views' (not 'pages') to prevent
# Streamlit's automatic multipage file-based routing from loading these files
# directly, which would bypass the login, CSS, and session state setup in app.py.
